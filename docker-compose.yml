services:
  zing-zest:
    build: .
    container_name: zing-zest-platform
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    environment:
      NODE_ENV: production
    restart: unless-stopped
